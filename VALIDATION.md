# 🛡️ Защита от некорректного ввода (Валидация)

## ✅ Реализованные валидаторы

### 1. **Номер телефона** (`handlers/auth.py`)

**Проверки:**
- ✅ Только цифры (без букв)
- ✅ Автонормализация: `8` → `+7`, `7` → `+7`
- ✅ Формат: `+7XXXXXXXXXX` (12 символов)
- ✅ Должен начинаться с `+7`

**Примеры:**
```
89991234567   → +79991234567  ✅
+79991234567  → +79991234567  ✅
79991234567   → +79991234567  ✅
+7(999)123-45-67 → +79991234567  ✅
1234567       → ❌ Неверная длина
+79991234abc  → ❌ Только цифры
```

---

### 2. **Токен бота** (`handlers/settings.py`)

**Проверки:**
- ✅ Формат: `число:строка`
- ✅ ID бота должен быть числом
- ✅ Секрет минимум 30 символов
- ✅ Содержит `:` разделитель

**Примеры:**
```
123456:ABC-DEF1234ghijklmno...  ✅
123456ABC-DEF                    ❌ Нет двоеточия
abc:ABC-DEF                      ❌ ID не число
123456:ABC                       ❌ Секрет слишком короткий
```

---

### 3. **Chat ID** (`handlers/settings.py`)

**Проверки:**
- ✅ Должен быть числом
- ✅ Не может быть нулевым
- ✅ Отрицательные ID допустимы (групповые чаты)

**Примеры:**
```
338908929    ✅
-100123456   ✅ (групповой чат)
0            ❌ Не может быть нулевым
abc123       ❌ Должен быть числом
```

---

### 4. **Дата** (`handlers/workers.py`)

**Проверки:**
- ✅ Формат: `YYYY-MM-DD`
- ✅ Не раньше 2020-01-01
- ✅ Не позже текущей даты + 2 года
- ✅ Реальная существующая дата

**Примеры:**
```
2026-02-05   ✅
2019-12-31   ❌ Слишком старая
2030-01-01   ❌ Слишком далеко в будущем
2026-02-30   ❌ Несуществующая дата
2026/02/05   ❌ Неверный формат
```

---

### 5. **Диапазон дат** (`handlers/workers.py`)

**Проверки:**
- ✅ date_from не позже date_to
- ✅ Максимальный диапазон: 365 дней
- ✅ Обе даты валидны

**Примеры:**
```
2026-02-05 → 2026-02-10  ✅
2026-02-10 → 2026-02-05  ❌ Начало позже конца
2026-01-01 → 2027-12-31  ❌ Диапазон > 365 дней
```

---

### 6. **Цена** (`handlers/workers.py`)

**Проверки:**
- ✅ Должна быть числом
- ✅ Не может быть отрицательной
- ✅ Максимум: 10,000,000
- ✅ Ноль допустим (с предупреждением)

**Примеры:**
```
2000          ✅
0             ✅ (с предупреждением)
-100          ❌ Отрицательная
999999999999  ❌ Слишком большая
abc           ❌ Не число
```

---

### 7. **Диапазон цен** (`handlers/workers.py`)

**Проверки:**
- ✅ min_price не больше max_price
- ⚠️ Предупреждение если 0-0

**Примеры:**
```
2000 → 5000  ✅
5000 → 2000  ❌ Минимум больше максимума
0 → 0        ⚠️ Может не дать результатов
```

---

### 8. **Username** (`handlers/blacklist.py`)

**Проверки:**
- ✅ Автоматически добавляет `@`
- ✅ Формат: латиница, цифры, подчёркивание
- ✅ Длина: 5-32 символа (без @)
- ✅ Без пробелов

**Примеры:**
```
@username     ✅
username      → @username  ✅
user_name123  → @user_name123  ✅
пользователь  ❌ Только латиница
us            ❌ Слишком короткий
@user name    ❌ Без пробелов
```

---

### 9. **Список чатов** (`handlers/workers.py`)

**Проверки:**
- ✅ Не пустой (минимум 1 чат)
- ✅ Максимум 50 чатов
- ✅ Каждый чат валидируется как username
- ✅ Удаление дубликатов
- ✅ Нормализация (добавление @)

**Примеры:**
```
@pvz_zamena
@pvz_jobs       ✅ (2 чата)

@pvz_zamena
@pvz_zamena     ✅ (дубликат удалён, остался 1)

                ❌ Пустой список

50+ чатов       ❌ Максимум 50
```

---

### 10. **URL** (`handlers/realty.py`)

**Проверки:**
- ✅ Начинается с `http://` или `https://`
- ✅ Содержит нужный домен (`avito.ru` или `cian.ru`)
- ✅ Не пустой

**Примеры:**
```
https://www.avito.ru/moskva/...        ✅
https://cian.ru/cat.php?...            ✅
www.avito.ru/...                       ❌ Нет протокола
https://othersite.ru/...               ❌ Неверный домен
```

---

### 11. **Количество страниц** (`handlers/realty.py`)

**Проверки:**
- ✅ Должно быть числом
- ✅ Минимум: 1
- ✅ Максимум: 100

**Примеры:**
```
3      ✅
50     ✅
0      ❌ Минимум 1
101    ❌ Максимум 100
abc    ❌ Не число
```

---

## 🚫 Защита от спама кнопок (AntiSpam)

### Механизм:

```python
class AntiSpam:
    """Cooldown между нажатиями кнопок"""

    # Хранилище: {user_id: {callback_data: timestamp}}
    _last_clicks = {}

    @staticmethod
    def check_and_update(user_id, callback_data, cooldown=1.0):
        """Проверить можно ли обработать нажатие (cooldown 1 сек)"""
```

### Использование:

```python
async def view_task(update: Update, context: ContextTypes.DEFAULT_TYPE):
    query = update.callback_query
    user_id = update.effective_user.id

    # Проверка спама
    if not AntiSpam.check_and_update(user_id, query.data, cooldown=1.0):
        await query.answer("⏳ Подождите немного...", show_alert=False)
        return

    # Обработка...
```

**Эффект:**
- Пользователь может нажимать кнопку максимум 1 раз в секунду
- Предотвращает случайные двойные клики
- Защита от "мусорных" запросов к API

---

## 📋 Где применяются валидаторы

### `handlers/auth.py`
- ✅ Валидация номера телефона

### `handlers/settings.py`
- ✅ Валидация токена бота (workers)
- ✅ Валидация Chat ID (workers)
- ✅ Валидация токена бота (realty)
- ✅ Валидация Chat ID (realty)

### `handlers/workers.py`
- ✅ Валидация списка чатов
- ✅ Валидация даты начала
- ✅ Валидация даты окончания + диапазона
- ✅ Валидация минимальной цены
- ✅ Валидация максимальной цены + диапазона

### `handlers/realty.py`
- ✅ Валидация URL (Avito)
- ✅ Валидация URL (Cian)
- ✅ Валидация количества страниц

### `handlers/blacklist.py`
- ✅ Валидация username (проверка)
- ✅ Валидация username (добавление чата)

---

## 🔒 Дополнительные защиты

### 1. **Обработка ошибок API**

Все вызовы к микросервисам обёрнуты в `try-except`:

```python
try:
    result = await workers_api.start_monitoring(...)
except httpx.HTTPError as e:
    logger.error(f"Ошибка API: {e}")
    await query.edit_message_text(
        "❌ Сервис временно недоступен. Попробуйте позже."
    )
```

### 2. **Проверка настроек перед запуском**

```python
if not settings.workers_bot_token or not settings.workers_chat_id:
    await query.edit_message_text(
        "❌ Не настроены уведомления.\n\n"
        "Перейдите в Настройки..."
    )
    return ConversationHandler.END
```

### 3. **Проверка авторизации**

```python
if not user.is_parser_authorized:
    await query.edit_message_text(
        "❌ Необходимо авторизовать аккаунт парсера."
    )
    return
```

### 4. **Удаление чувствительных данных**

```python
# Удаляем сообщение с номером телефона
try:
    await update.message.delete()
except:
    pass
```

---

## 📊 Сценарии защиты

### ❌ БЕЗ валидации:
```
Пользователь вводит: "8999abc1234"
→ Pyrogram: FloodWait / InvalidPhone
→ Бот падает / показывает техническую ошибку
```

### ✅ С валидацией:
```
Пользователь вводит: "8999abc1234"
→ Валидатор: "❌ Номер должен содержать только цифры"
→ Пользователь видит понятное сообщение
→ Может исправить ошибку
```

---

## 🎯 Итого

### Защищены все пользовательские вводы:
- ✅ 11 типов валидации
- ✅ Защита от спама кнопок
- ✅ Обработка ошибок API
- ✅ Проверка состояний перед действиями
- ✅ Удаление чувствительных данных

### Результат:
- 🛡️ Бот не упадёт от "дурацкого" ввода
- 📝 Понятные сообщения об ошибках
- 🔒 Защита от случайных повторных нажатий
- ⚡ Предотвращение "мусорных" запросов к API
