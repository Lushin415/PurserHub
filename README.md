# ScanBot (PurserHub)

## 1. Описание

**ScanBot** (известный также как PurserHub) — это комплексная микросервисная система для автоматизации мониторинга Telegram-чатов и популярных досок объявлений (Avito, ЦИАН). Проект разработан для облегчения поиска вакансий/работников ПВЗ, проверки соискателей/работодателей по чёрным спискам и отслеживания новых объявлений о недвижимости.

Система предназначена для:
- Владельцев пунктов выдачи заказов (ПВЗ), ищущих сотрудников или отслеживающих недобросовестных работников.
- HR-специалистов и рекрутеров в сфере логистики и e-commerce.
- Агентов по недвижимости и инвесторов для мониторинга рынка.
- Всех, кто хочет автоматизировать рутинный поиск информации в Telegram и на классифайдах.

---

## 2. Возможности

### Мониторинг вакансий и резюме ПВЗ в Telegram-чатах
- Поиск объявлений о работе или работниках ПВЗ по заданным критериям.
- Фильтрация по географии (Москва, Санкт-Петербург, Все источники).
- Фильтрация по датам (предполагаемая дата выхода на работу).
- Фильтрация по ставке (минимальная и максимальная оплата за смену).
- Поддержка глобальных чатов и конкретных топиков форумов (формат `@chat/topic_id`).
- Уведомления о найденных объявлениях прямо в Telegram-бот.
- Двухуровневая дедупликация объявлений (по хешу контента и по автору).
- Автоматическое переподключение при потере интернета.

### Проверка пользователей по Чёрному списку ПВЗ
- **Трёхступенчатый поиск** — максимальное покрытие даже если человек сменил никнейм:
  1. Поиск по `@username` — быстрое совпадение по нику в текстах записей ЧС.
  2. Поиск по User ID — система автоматически определяет числовой ID аккаунта и ищет по нему. Позволяет найти человека даже после смены никнейма.
  3. Поиск по ФИО — если известна фамилия (или фамилия и имя, или полное ФИО), система проверит наличие этих слов в записях ЧС.
- Поддержка поиска в топиках форумов Telegram (raw API `GetReplies`).
- Поддержка сообщений с медиа (фото + текст в `caption`).
- Поиск нескольких ID в одном сообщении.
- Вывод информации о найденных совпадениях (ФИО, телефон, способ нахождения).
- **Неблокирующий поиск**: бот остаётся отзывчивым пока идёт поиск.
- Интегрированная кнопка «Проверить в ЧС» в уведомлениях о новых вакансиях.

### Мониторинг объявлений о недвижимости (Avito, ЦИАН)
- Запуск мониторинга по URL-адресам (Avito, ЦИАН или оба сразу).
- Получение уведомлений о новых объявлениях.
- Настройка прокси-серверов и URL ротации IP через AdminPanel.
- Перезапуск сервиса недвижимости через AdminPanel.

### Управление Telegram-аккаунтами
- Авторизация личных Telegram-аккаунтов через Pyrogram (MTProto).
- Две независимые сессии на пользователя: парсер ПВЗ и чёрный список.
- Поддержка двухфакторной аутентификации (2FA).
- Отключение отдельных или всех авторизованных аккаунтов.

### Система подписок
- Просмотр статуса активной подписки.
- Тарифы: 1 день, 30 дней, 90 дней.
- Платёжная интеграция (YooKassa и другие провайдеры через `@BotFather`).

### Панель администратора
- Просмотр и управление подписками пользователей.
- Выдача подписок вручную (по Telegram ID).
- Управление ценами тарифов.
- Статистика доходов (сегодня, за месяц, всего).
- Управление списком администраторов.
- Настройка чатов мониторинга ПВЗ (изменить / очистить).
- Настройка чатов чёрного списка с синхронизацией в Workers Service.
- Настройка прокси для парсера недвижимости.

---

## 3. Архитектура

Проект построен на микросервисной архитектуре — три компонента в Docker-контейнерах, взаимодействующие через HTTP API и общий Docker-том для Pyrogram-сессий.

```
┌──────────────────────┐    HTTP API    ┌─────────────────────┐
│    PurserHub Bot     │ ─────────────▶ │   Workers Service   │
│  (python-telegram-bot│ ◀───────────── │   (FastAPI/Pyrogram) │
│   + Pyrogram auth)   │                │                     │
│                      │                │ • Мониторинг ПВЗ    │
│  • Управление        │    HTTP API    │ • Поиск в ЧС        │
│  • Авторизация       │ ─────────────▶ │ • Уведомления       │
│  • SQLite БД         │                └──────────┬──────────┘
│  • AdminPanel        │                           │ Pyrogram
│                      │                           │ (shared sessions)
│                      │    HTTP API    ┌──────────▼──────────┐
│                      │ ─────────────▶ │   Realty Monitor    │
└──────────────────────┘                │ (parser Avito/CIAN) │
          │                             └─────────────────────┘
          │ Docker Volume
          ▼ (shared_sessions)
 ┌────────────────────┐
 │  Pyrogram Sessions │
 │ {user_id}_parser   │
 │ {user_id}_blacklist│
 └────────────────────┘
```

### Передача данных между компонентами

| Канал | Назначение |
|---|---|
| HTTP REST API | `PurserHub` → `Workers Service` / `Realty Monitor`: запуск/остановка задач, статусы |
| Telegram Bot API | `Workers Service` / `Realty Monitor` → пользователи: уведомления (через `BOT_TOKEN`) |
| Docker Volume `shared_sessions` | Pyrogram-сессии, созданные `PurserHub`, доступны `Workers Service` без повторной авторизации |
| SQLite (parserhub.db) | Пользователи, задачи, подписки, AdminPanel-конфиг |
| SQLite (workers.db) | Найденные объявления, чаты ЧС |

---

## 4. Технологии

| Технология | Назначение |
|---|---|
| Python 3.11+ | Основной язык |
| python-telegram-bot 20.x | Telegram-бот (PurserHub) |
| Pyrogram 2.x | MTProto API (авторизация, парсинг) |
| FastAPI | REST API (Workers Service, Realty Monitor) |
| aiosqlite | Асинхронная SQLite БД |
| Loguru | Логирование |
| Pydantic v2 | Валидация данных и конфигурация |
| httpx | Async HTTP-клиент |
| Docker / Docker Compose | Контейнеризация |

---

## 5. Требования

### Системные
- Docker Engine 24+
- Docker Compose v2

### Учётные данные

| Credential | Где получить | Обязательно |
|---|---|---|
| `BOT_TOKEN` | `@BotFather` → `/newbot` | Да |
| `API_ID` + `API_HASH` | [my.telegram.org](https://my.telegram.org/) → API Development tools | Да |
| `ADMIN_ID` | Ваш Telegram ID (узнать у `@userinfobot`) | Да |
| `PROVIDER_TOKEN` | `@BotFather` → `/mybots` → платёжный провайдер | Нет |

---

## 6. Установка и запуск

### Шаг 1. Клонировать репозиторий

```bash
git clone https://github.com/Lushin415/PurserHub.git
cd PurserHub
```

### Шаг 2. Получить Telegram API ID и API Hash

1. Перейдите на [my.telegram.org](https://my.telegram.org/)
2. Войдите в свой аккаунт (тот, от имени которого будет работать парсер)
3. Перейдите в «API Development tools»
4. Создайте приложение — получите `App api_id` и `App api_hash`

### Шаг 3. Создать и настроить .env файл

```bash
cp .env.example .env
```

Откройте `.env` и заполните обязательные поля:

```ini
BOT_TOKEN=ВАШ_ТОКЕН_БОТА
ADMIN_ID=ВАШ_TELEGRAM_ID
API_ID=ВАШ_API_ID
API_HASH=ВАШ_API_HASH
```

### Шаг 4. Запустить через Docker Compose

```bash
docker compose up --build -d
```

Проверить статус:
```bash
docker compose ps
```

Посмотреть логи:
```bash
docker compose logs -f parserhub
docker compose logs -f workers-service
```

> **Важно**: При изменении кода пересобирайте нужный сервис:
> ```bash
> docker compose build parserhub --no-cache
> docker compose up -d parserhub
> ```

---

## 7. Конфигурация

### PurserHub (`PurserHub/.env`)

| Переменная | Описание | Обязательна |
|---|---|---|
| `BOT_TOKEN` | Токен Telegram-бота от `@BotFather` | Да |
| `ADMIN_ID` | Telegram ID главного администратора | Да |
| `API_ID` | Telegram API ID с my.telegram.org | Да |
| `API_HASH` | Telegram API Hash с my.telegram.org | Да |
| `WORKERS_SERVICE_URL` | URL Workers Service (переопределяется Docker) | Да |
| `REALTY_SERVICE_URL` | URL Realty Monitor (переопределяется Docker) | Да |
| `PROVIDER_TOKEN` | Токен провайдера платежей (YooKassa и др.) | Нет |
| `DB_PATH` | Путь к SQLite БД (переопределяется Docker) | Нет |
| `SESSIONS_DIR` | Директория Pyrogram-сессий (переопределяется Docker) | Нет |
| `LOG_PATH` | Путь к файлу логов (переопределяется Docker) | Нет |

> **Примечание**: `WORKERS_SERVICE_URL`, `REALTY_SERVICE_URL`, `DB_PATH`, `SESSIONS_DIR`, `LOG_PATH` задаются в `.env`, но в `docker-compose.yml` **автоматически переопределяются** значениями для внутренней Docker-сети и монтированных томов.

### Workers Service

Конфигурируется автоматически через `docker-compose.yml`. Следующие параметры **передаются при запуске задачи** от PurserHub, а не через `.env`:
- `BOT_TOKEN` — для отправки уведомлений
- `API_ID` / `API_HASH` — для Pyrogram
- Пути к сессиям, список чатов, фильтры

---

## 8. Использование

### Команды бота

| Команда | Описание |
|---|---|
| `/start` | Главное меню бота |
| `/admin` | Панель администратора (только для admins) |

### Главное меню

#### 👤 Мой аккаунт
- Статус подключения аккаунтов парсера ПВЗ и чёрного списка
- **🔑 Авторизация парсера ПВЗ** — подключить Telegram-аккаунт для мониторинга чатов
- **🔑 Авторизация Чёрного списка** — подключить Telegram-аккаунт для поиска в ЧС
- **🔓 Отключить** — удалить конкретную сессию
- **⛔ Отключить все аккаунты** — удалить все Pyrogram-сессии

#### 👷 Мониторинг ПВЗ
Требует авторизованный аккаунт парсера.

**🚀 Запустить мониторинг:**
1. Выбор режима: `👷 Работники` или `🏢 Работодатели`
2. Выбор города: `🏙 Москва`, `🌊 Санкт-Петербург`, `🌍 Все источники`
3. Фильтр по датам (предполагаемая дата выхода, формат ДД.ММ.ГГГГ)
4. Фильтр по ставке (мин/макс, руб/смена)
5. Подтверждение и запуск

**📋 Задачи мониторинга:**
- Список активных задач со статусом
- Просмотр статистики задачи (просканировано / найдено / отправлено)
- Остановка задачи или принудительное завершение

**Кнопки в уведомлениях:**
- `Проверить в ЧС` — моментальная проверка автора в чёрном списке
- `Игнорировать` — скрыть уведомление

#### ⚫ Чёрный список
Требует авторизованный аккаунт ЧС.

**🔍 Проверить пользователя** — ввод `@username`, результат придёт автоматически (фоновый поиск, бот остаётся отзывчивым)

#### 🏠 Мониторинг недвижимости
- Выбор источника: Avito, ЦИАН или оба
- Ввод URL объявлений (с сортировкой по дате)
- Запуск/просмотр/остановка задач

#### 💳 Подписка
- Просмотр текущего статуса подписки
- Переход к оплате (если настроен провайдер)

### Панель администратора (`/admin`)

| Раздел | Возможности |
|---|---|
| 📋 Подписки | Просмотр всех активных подписок (ID, username, тариф, срок) |
| 🎁 Выдать подписку | Выдача вручную по Telegram ID, выбор тарифа |
| 💰 Цены подписок | Просмотр и изменение стоимости тарифов |
| 💰 Доходы | Статистика (сегодня / месяц / всего) и количество активных подписок |
| 👥 Администраторы | Список, добавление (по @username или ID), удаление |
| 📝 Чаты ПВЗ | Просмотр / ✏️ Изменить список / 🗑 Очистить список |
| 📝 Чаты ЧС | Просмотр / ✏️ Изменить список / 🗑 Очистить список (синхронизируется с Workers Service) |
| 🌐 Настройки прокси | Текущий прокси, изменить, удалить, перезапустить сервис недвижимости |

> **Формат чатов с топиками**: Для Telegram-форумов используйте формат `@chatname/topic_id`, например: `@pvz_zamena/912`

---

## 9. Структура проекта

```
PurserHub/
├── parserhub/                    # Главный Telegram-бот
│   ├── bot.py                    # Точка входа, регистрация обработчиков
│   ├── config.py                 # Конфигурация из переменных окружения
│   ├── db_service.py             # Сервис SQLite (пользователи, задачи, подписки, конфиг)
│   ├── session_manager.py        # Управление Pyrogram-сессиями
│   ├── api_client.py             # HTTP-клиенты для Workers Service и Realty Monitor
│   ├── models.py                 # Pydantic-модели
│   ├── validators.py             # Валидаторы ввода
│   ├── services/
│   │   └── subscription_service.py  # Логика подписок и платежей
│   └── handlers/
│       ├── admin.py              # AdminPanel
│       ├── auth.py               # Авторизация Pyrogram-аккаунтов
│       ├── blacklist.py          # Поиск в чёрном списке
│       ├── realty.py             # Мониторинг недвижимости
│       ├── workers.py            # Мониторинг ПВЗ
│       ├── settings.py           # Настройки пользователя
│       └── start.py              # /start, главное меню
├── Dockerfile
├── docker-compose.yml            # Конфигурация всех микросервисов
└── .env.example                  # Шаблон переменных окружения

workers_service/
├── api.py                        # FastAPI REST API
├── config.py                     # Конфигурация Workers Service
├── db_service.py                 # SQLite (задачи, найденные объявления, чаты ЧС)
├── parser.py                     # Pyrogram-парсер Telegram-чатов
├── blacklist_service.py          # Поиск в чатах чёрного списка
├── tg_notifier.py                # Отправка уведомлений через Bot API
├── tasks.py                      # Фоновые задачи мониторинга
├── state_manager.py              # Управление состоянием задач
├── message_extractor.py          # Извлечение данных из сообщений
├── filters.py                    # Фильтрация объявлений
├── deduplicator.py               # Дедупликация объявлений
├── models_api.py                 # Pydantic-модели API
├── models_db.py                  # Pydantic-модели БД
└── Dockerfile

parser_avito_cian/                # Парсер недвижимости
├── config.toml                   # Настройки парсера
└── Dockerfile
```

---

## 10. API Workers Service

Base URL: `http://workers-service:8002` (внутри Docker-сети)

### Мониторинг ПВЗ

| Метод | Эндпоинт | Описание |
|---|---|---|
| `POST` | `/workers/start` | Запустить задачу мониторинга |
| `GET` | `/workers/status/{task_id}` | Статус и статистика задачи |
| `POST` | `/workers/stop/{task_id}` | Остановить задачу |
| `GET` | `/workers/list/{task_id}` | Список найденных объявлений |
| `POST` | `/workers/{item_id}/check-blacklist` | Проверить автора объявления в ЧС |

#### POST /workers/start — тело запроса

```json
{
  "user_id": 123456789,
  "mode": "worker",
  "chats": ["@pvz_workers", "@pvz_zamena/912"],
  "filters": {
    "date_from": "2024-01-01",
    "date_to": "2024-12-31",
    "min_price": 2000,
    "max_price": 5000,
    "shk_filter": "любое",
    "city_filter": "МСК"
  },
  "session_path": "/app/sessions/123456789_parser",
  "blacklist_session_path": "/app/sessions/123456789_blacklist",
  "notification_chat_id": 123456789,
  "parse_history_days": 3
}
```

### Чёрный список

| Метод | Эндпоинт | Описание |
|---|---|---|
| `POST` | `/blacklist/check` | Проверить username в ЧС |
| `GET` | `/blacklist/chats` | Список чатов ЧС |
| `POST` | `/blacklist/chats/sync` | Полная замена списка чатов ЧС |
| `POST` | `/blacklist/chats/add` | Добавить чат в ЧС |
| `POST` | `/blacklist/chats/remove` | Удалить чат из ЧС |
| `GET` | `/blacklist/chats/topics` | Топики форума (для выбора топика) |

### Администрирование

| Метод | Эндпоинт | Описание |
|---|---|---|
| `GET` | `/admin/stats` | Статистика БД |
| `POST` | `/admin/cleanup` | Очистка старых записей |
| `GET` | `/health` | Healthcheck |

---

## 11. Pyrogram-сессии и общий том

### Принцип работы

Каждый пользователь авторизует до 2 Telegram-аккаунтов:

| Сессия | Назначение | Файл |
|---|---|---|
| `{user_id}_parser.session` | Мониторинг чатов ПВЗ | `/app/data/sessions/` |
| `{user_id}_blacklist.session` | Поиск в чатах ЧС | `/app/data/sessions/` |

### Docker Volume `shared_sessions`

```yaml
# docker-compose.yml (упрощённо)
services:
  parserhub:
    volumes:
      - shared_sessions:/app/data/sessions   # создаёт сессии

  workers-service:
    volumes:
      - shared_sessions:/app/sessions        # читает те же сессии

volumes:
  shared_sessions:
```

**Преимущества:**
- Авторизация один раз в `PurserHub` → сессии доступны всем микросервисам
- Сессии сохраняются при перезапуске контейнеров
- `Workers Service` работает от имени пользователя без повторного входа

### Важно при авторизации

> Telegram отправляет код подтверждения **в приложение** (`SentCodeType.APP`). Скопируйте код и вставьте в бота через пробелы!.

---

## 12. Troubleshooting

### `AUTH_KEY_UNREGISTERED` / `AUTH_KEY_INVALID`

**Симптом**: Мониторинг или поиск в ЧС внезапно прекращают работу, бот присылает уведомление об ошибке.

**Причина**: Telegram аннулировал сессию (вход с другого устройства, долгая неактивность и т.д.).

**Решение**: «👤 Мой аккаунт» → авторизоваться заново для нужного типа сессии.

---

### `PHONE_CODE_EXPIRED` сразу после ввода кода

**Причина**: Вы ввели код из официального Telegram-клиенте без пробелов.

**Решение**: Дождитесь нового кода, скопируйте код и вставьте в бота **с пробелами между цифр**.

---

### `USERNAME_INVALID` при поиске в ЧС

**Симптом**: `Ошибка доступа к чату @chat/topicid: USERNAME_INVALID`

**Причина**: Чат сохранён в БД в старом формате `@chat/topic_id` вместо разделённых полей.

**Решение**: AdminPanel → 📝 Чаты ЧС → ✏️ Изменить список → заново введите чаты. При сохранении формат нормализуется автоматически.

---

### Задача не видна / недоступна после перезапуска сервиса

**Причина**: Активные задачи хранятся в памяти `Workers Service`. При перезапуске контейнера они теряются, но в `parserhub.db` остаются как «running».

**Решение**: «📋 Задачи мониторинга» → 🗑 Принудительно завершить → запустить новую задачу.

---

### Ошибка `Form data requires "python-multipart"`

**Причина**: Неверная аннотация параметра в FastAPI-эндпоинте.

**Решение**: Убедитесь, что тело запроса аннотировано `Body(...)`:
```python
from fastapi import Body
async def sync_blacklist_chats(chats: list = Body(...)):
    ...
```

---

### Сервис недоступен / HTTP ошибки

```bash
# Проверить статус контейнеров
docker compose ps

# Посмотреть логи
docker compose logs -f workers-service
docker compose logs -f parserhub

# Перезапустить конкретный сервис
docker compose restart workers-service

# Пересобрать и перезапустить всё
docker compose up --build -d
```

---

### Нет чатов для мониторинга ПВЗ

**Причина**: Администратор не добавил глобальные чаты.

**Решение**: `/admin` → 📝 Чаты ПВЗ → ✏️ Изменить список → ввести чаты построчно:
```
@pvz_workers
@pvz_jobs
@pvz_zamena/912
```

---

## Лицензия

Проект предназначен для частного использования.
